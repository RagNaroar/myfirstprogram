#include <stdio.h>

int main(){
    int width = 6;
    int length = 4;
    int area;

    //определение ширины площади
    area = length * width;

//расчеты//
    printf("width is: %d\n", width);
    printf("length is: %d\n", length);
    printf("area calculate: %d\n", area);

    return 0;
}